os: Visual Studio 2015

environment:
  matrix:
    - target: x86_64-pc-windows-msvc
    #- target: i686-pc-windows-msvc
    #- target: x86_64-pc-windows-gnu
    #- target: i686-pc-windows-gnu

install:
  - sh: echo $SHELL
  - sh: echo This is the shell! $TERM

build: false

test_script:
  - sh: echo HELLO!
  - sh: type echo
